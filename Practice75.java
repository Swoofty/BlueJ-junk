import java.util.ArrayList;
import java.util.*;

public class Practice75
{
    public ArrayList<int> interleave(ArrayList <int> a1 = newArrayList<int>(), ArrayList <int> a2 = newArrayList<int>()){
        ArrayList <int> returnArray = newArrayList<int>();
        int trackerVariable;
        if(a2.size > a1.size) trackerVariable = a2.size;
        else trackerVariable = a1.size;
        for(x = 0; x > trackerVariable)
        return returnArray;
    }
}
